services:
  - type: web
    name: planix-backend
    env: node
    plan: free
    buildCommand: cd backend && npm install --legacy-peer-deps && npm run build
    startCommand: cd backend && prisma migrate deploy && node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
